# Heroku Environment Variables Configuration
# Set these in your Heroku dashboard under Settings > Config Vars

# MongoDB Connection
MONGODB_URI=mongodb+srv://your-username:your-password@your-cluster.mongodb.net/tapro?retryWrites=true&w=majority

# JWT Secret (generate a strong random string)
JWT_SECRET=your-super-secret-jwt-key-here

# CORS Configuration
CORS_ORIGIN=https://your-frontend-domain.netlify.app
FRONTEND_URL=https://your-frontend-domain.netlify.app

# Node Environment
NODE_ENV=production

# Port (Heroku will set this automatically)
PORT=5000
